<Window x:Class="ScreenWarden.VoiceCommandEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Voice Command" Height="220" Width="400" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Background="#232323">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Phrase:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Foreground="White"/>
        <TextBox x:Name="PhraseTextBox" Grid.Row="0" Grid.Column="1" Margin="0,4" />

        <TextBlock Text="Action:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Foreground="White"/>
        <ComboBox x:Name="ActionComboBox" Grid.Row="1" Grid.Column="1" Margin="0,4" />

        <CheckBox x:Name="EnabledCheckBox" Content="Enabled" Grid.Row="2" Grid.Column="1" Margin="0,8,0,0" Foreground="White"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="OK" Width="80" Margin="0,0,8,0" IsDefault="True" Click="OkButton_Click"/>
            <Button Content="Cancel" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
