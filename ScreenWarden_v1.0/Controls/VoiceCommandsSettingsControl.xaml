<UserControl x:Class="ScreenWarden.Controls.VoiceCommandsSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ScreenWarden.Controls">
    <UserControl.Resources>
        <local:BoolToTypeConverter x:Key="BoolToTypeConverter"/>
    </UserControl.Resources>
    
    <Border Background="#252525" BorderBrush="#444444" BorderThickness="1" CornerRadius="3" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" Text="Configure voice commands:" Foreground="#AAAAAA" FontSize="12" Margin="0,0,0,8"/>
            
            <ListBox x:Name="CommandsListView" Grid.Row="1" 
                     SelectionChanged="CommandsListView_SelectionChanged"
                     Background="#2D2D2D" BorderBrush="#3D3D3D" BorderThickness="1"
                     ScrollViewer.VerticalScrollBarVisibility="Auto" 
                     MaxHeight="160">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="5,4"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#0078D4"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3A3A3A"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="25"/>
                                <ColumnDefinition Width="145"/>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="55"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox Grid.Column="0" IsChecked="{Binding IsEnabled, Mode=TwoWay}" 
                                      VerticalAlignment="Center"
                                      Checked="CommandCheckBox_Changed" Unchecked="CommandCheckBox_Changed"/>
                            <TextBlock Grid.Column="1" Text="{Binding Phrase}" Foreground="White" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2" Text="{Binding Action}" Foreground="#BBBBBB" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="3" Text="{Binding IsBuiltIn, Converter={StaticResource BoolToTypeConverter}}" 
                                       Foreground="#888888" FontSize="11" VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            
            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,0">
                <Button Content="Add" Padding="10,5" Margin="0,0,5,0" Click="AddCommand_Click"
                        Background="#4A4A4A" Foreground="White" BorderBrush="#5A5A5A"/>
                <Button x:Name="EditButton" Content="Edit" Padding="10,5" Margin="0,0,5,0" 
                        Click="EditCommand_Click" IsEnabled="False"
                        Background="#2A2A2A" Foreground="#555555" BorderBrush="#3A3A3A"/>
                <Button x:Name="DeleteButton" Content="Delete" Padding="10,5" Margin="0,0,5,0" 
                        Click="DeleteCommand_Click" IsEnabled="False"
                        Background="#2A2A2A" Foreground="#555555" BorderBrush="#3A3A3A"/>
                <Button Content="Reset" Padding="10,5" Margin="0,0,15,0" Click="ResetDefaults_Click"
                        Background="#4A4A4A" Foreground="White" BorderBrush="#5A5A5A"/>
                <Button Content="Save" Padding="14,5" Click="SaveChanges_Click"
                        Background="#0078D4" Foreground="White" BorderBrush="#0078D4" FontWeight="SemiBold"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
